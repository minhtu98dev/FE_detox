export const useOrder = () => {
    const getOrder = () => { } // * Lấy danh sách đơn hàng
    const editOrder = () => { } // * Sửa đơn hàng
    const deleteOrder = () => { } // * Xoá đơn hàng
    const addOrder = () => { } // * Thêm đơn hàng

    return { getOrder, editOrder, deleteOrder, addOrder }
}